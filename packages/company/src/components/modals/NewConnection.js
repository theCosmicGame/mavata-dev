import React, { useState } from 'react';
import styled from 'styled-components';

export default function NewConnection({ company, onAddConnection, showModal, closeModal }) {
  return (
    <React.Fragment>

    </React.Fragment>
  )
}